import { Brand } from '@digiv3rse/shared-kernel';
export type PublicationId = Brand<string, 'PublicationId'>;
/**
 * @internal
 */
export declare function isMomokaPublicationId(id: PublicationId): boolean;
